plugins {
    id("java")
}

repositories {
    mavenCentral()
}

dependencies {
    implementation("org.telegram:telegrambots:6.9.7.1")   // стабильная версия TelegramBots
    implementation("com.google.code.gson:gson:2.11.0")    // JSON
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(23))
        // необязательно, но можно зафиксировать вендора:
        // vendor.set(JvmVendorSpec.AMAZON)
    }
}
